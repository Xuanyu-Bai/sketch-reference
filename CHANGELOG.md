# 修改历史

本项目经过 4 次主要 commit，演化过程如下。

## 1. `initial` — 原始骨架
基于 Three.js + GLTFLoader 的 3D 模型查看器：
- OrbitControls 鼠标拖拽旋转/缩放/平移
- 6 个预设视角（正面/3-4 侧/全侧/3-4 后/背面/顶视）
- 主光源（关键光）滑块
- 线框模式、坐标轴、水平镜像、原图对比
- 截图保存为 PNG
- 背景色预设

## 2. `feat: grid as fixed chassis + mouse-driven tracer perspective`
**重写相机控制 + 加底盘**
- 删掉 OrbitControls，换成自定义**鼠标驱动的球坐标相机**
  - 鼠标在画布上滑动 → 相机绕模型做轻微轨道运动（模拟写生时头部的微摆）
  - 鼠标移出画布 → 视角自动回正
  - 滚轮缩放（0.4 ~ 6 单位）
- 删掉原来孤零零的 GridHelper，新建**永久显示的底盘**：
  - 圆形暗灰基座（转盘感）
  - 香槟金高光圈
  - 4×4 主网格 + 20×20 远景网格
  - 模型降到底盘上，底部贴齐
- 加 **"临摹者视角"** 侧栏区：灵敏度 / 摆动范围 / 阻尼 三个滑块
- 加 **"底盘"** 侧栏区：显示/大小/不透明度
- 加 **"焦点对准"**：相机目标点 = 模型平移后真实中心
- 加 **HUD 小面板**（左上的 方位/仰角/距离）
- 加 **"临摹者准星"**：画布正中央的小圆环 + 十字（鼠标进入画布时浮现）
- 预设视角改成**球坐标**（az/el/dist）

## 3. `feat: reticle toggle, snap-to-center, fit-to-view, sidebar collapse`
**UX 大幅改善**
- **"显示准星"开关** 加到临摹者视角区（之前用户被这个圆环困惑）
- 帮助文字改写，说明"准星 = 视线中心，可关闭"
- **"对准模型中心"按钮**：平滑过渡到模型中心
- **"框选整个模型"按钮**：根据 FOV + 宽高比自动算距离，保证模型在两个方向都装得下
- **滚轮缩放上限** 6 → 60（很多扫描模型在 GLB 里单位偏大，6 装不下）
- 加 `fitDistanceToModel(size)` 函数：垂直 FOV 距离和水平 FOV 距离取较大者
- **"折叠工具栏"按钮**（⇥ 在画布右上角），收起后画布占满整屏
- 同步：原图文件名从 `original-sketch.png` 改为实际的 `natural.png`

## 4. `fix: NDC-normalized X/Y offset + focus on face area`
**真正修好"人物不在屏幕中心"**
- 修一个 bug：`view.modelBox.center` 之前存的是**模型平移之前**的包围盒中心，
  导致"对准中心"和"框选模型"按钮瞄准的是错的位置 → 模型被甩到画面边上
- **X/Y 偏移归一化**：把滑块从"世界单位"改成"以可见区域为基准"
  - 1.0 = 半个可见区域的 14%
  - 偏移沿**相机右向量 / 上向量**施加（不是世界 XY），转视角时模型在屏幕上的位置不变
  - 缩放工具栏时，相同滑块值产生相同的屏幕位置
- **焦点改成"人脸区域"**：从"包围盒几何中心"改为"包围盒顶部往下 25%"
  - 半身像的视觉中心（脸）自动落在画面正中央
- 加 **"焦点高度"滑块**（0% = 头顶，50% = 中间）：不同模型可以调到最适合脸的位置
- 模型加载时自动重置 X/Y 偏移到 0（新焦点的位置已经更准了）

## 关键经验

- **不要把"几何中心"当成"视觉中心"**：半身像的包围盒中心在胸口，脸在上面 30% 处
- **侧栏折叠/展开时，滑块值的物理意义不能变**：用归一化（NDC）而不是世界单位
- **写生练习需要稳定的视野**：相机看哪里，模型就该在哪里，不要让用户操心相机参数
