# Cloudflare Pages 配置
# 部署命令：npx wrangler pages deploy . --project-name=sketch-reference

name = "sketch-reference"
compatibility_date = "2025-09-01"
pages_build_output_dir = "."

# 不需要 build 步骤，因为我们是纯静态文件
# Pages 会自动从仓库根目录部署

# 环境变量（如果有）
# [vars]
# MESHY_API_KEY = "..."  # 仅服务端使用，客户端不要暴露
