# 素描临摹 · 3D 参考

一个面向美术生/写生练习的网页工具：

- 加载一个 **3D 头部/半身像模型**（GLB 格式）
- 通过鼠标模拟**写生者观察视角**的微调
- 模型稳稳站在**网格底盘**上，便于感知空间关系
- 调节**主光源**方向来控制明暗交界线（头像写生的核心）

## 快速开始

### Windows（推荐）

双击 `start.bat`，浏览器自动打开 `http://localhost:8000`

### 手动启动

任选其一：

```bash
# Python 3
python -m http.server 8000

# Node.js
npx serve .
```

然后浏览器访问 `http://localhost:8000`。

### 直接双击 index.html ⚠️

可以打开，但首次会弹出文件选择框（因为 `file://` 协议禁止 JS 读取本地文件）。选一次 GLB 即可。

## 文件结构

| 文件 | 用途 |
|---|---|
| `index.html` | 网站主体（所有功能都在这一个文件里） |
| `model.glb` | 你的 3D 模型（**自己提供**，不放仓库里） |
| `natural.png` | 原素描参考图（可选） |
| `start.bat` / `start.ps1` | 一键启动本地服务器 |
| `README.md` | 本文件 |
| `CHANGELOG.md` | 修改历史 |

## 主要功能

### 临摹者视角（鼠标驱动相机）

- **鼠标移动** → 相机围绕模型做轻微轨道运动，模拟写生时头部的微摆
- **移出画布** → 视角自动回正
- **滚轮** → 缩放距离（0.3 ~ 60 单位）
- **灵敏度 / 摆动范围 / 阻尼** 三个滑块调节手感

### 底盘（地面）

- 大型网格地面（4×4 主网格 + 20×20 远景网格）
- 暗灰色圆形基座 + 香槟金高光圈
- 单独控制显示/隐藏、底盘大小、不透明度

### 预设视角

正面 / 3/4 侧 / 全侧 / 3/4 后 / 背面 / 顶视，快捷键 F/S/T/R

### 主光源

模拟任意方向的关键光（水平角度 + 垂直仰角 + 强度 + 环境光）

### 辅助功能

- **线框模式**（快捷键 W）
- **水平镜像**
- **原图对比**：把 natural.png 显示在右上角对照
- **截图保存**：当前视角导出 PNG

## 切换模型

把任意 GLB 文件放到本目录，重命名为 `model.glb`，刷新页面即可。

或在右侧栏点 **"加载其他 GLB 文件"** 按钮选择其他位置的模型。

## 配置修改

打开 `index.html`，最上面 `<script type="module">` 块里的 `CONFIG` 对象：

```js
const CONFIG = {
  modelFile: 'model.glb',          // 默认加载的模型文件名
  originalSketch: 'natural.png',   // 原图对比的图
  title: '素描临摹 · 3D 参考模型',
  defaultBackground: '#f5f3ee',    // 画布背景色
};
```

## 浏览器要求

需要支持 ES Modules + importmap 的现代浏览器：

- Chrome / Edge 89+
- Firefox 108+
- Safari 16.4+

## License

MIT
